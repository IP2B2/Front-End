
import { Inter } from 'next/font/google'

export const InterHeading = Inter({
  subsets: ['latin'],
  weight: '700'
});
export const InterEmphasis = Inter({
    subsets: ['latin'],
    weight: '600'
});
export const InterText = Inter({
    subsets: ['latin'],
    weight: '500'
});
export const InterLight = Inter({
    subsets: ['latin'],
    weight: '400'
});